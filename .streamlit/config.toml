# Tema "LinkedIn" do dashboard — vale para execução local e Docker (Makefile monta o repo).
[theme]
base = "light"
primaryColor = "#0A66C2"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F3F6F8"
textColor = "#1D1D1F"

[browser]
gatherUsageStats = false
